import React from 'react';
import { Typography } from 'antd';

function Home(props) {
   return (
      <div>
         <Typography style={{ textAlign: 'center' }}>Welcome to the Estate Agent homepage, we have loads of properties in store for you!</Typography>
      </div>
   )
}

export default Home;